<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.jejuro.miraero.domain.aicoach.mapper.AiCoachConversationMapper">

    <resultMap id="aiCoachConversationResultMap"
               type="org.jejuro.miraero.domain.aicoach.domain.AiCoachConversation">
        <id property="aiCoachConversationId" column="ai_coach_conversation_id"/>
        <result property="userId" column="user_id"/>
        <result property="title" column="title"/>
        <result property="conversationSummary" column="conversation_summary"/>
        <result property="lastMessageAt" column="last_message_at"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <sql id="conversationColumns">
        ai_coach_conversation_id,
        user_id,
        title,
        conversation_summary,
        last_message_at,
        created_at,
        updated_at
    </sql>

    <select id="findLatestByUserId" resultMap="aiCoachConversationResultMap">
        SELECT
            <include refid="conversationColumns"/>
        FROM ai_coach_conversation
        WHERE user_id = #{userId}
        ORDER BY last_message_at DESC, created_at DESC
        LIMIT 1
    </select>

    <select id="findAllByUserId" resultMap="aiCoachConversationResultMap">
        SELECT
            <include refid="conversationColumns"/>
        FROM ai_coach_conversation
        WHERE user_id = #{userId}
        ORDER BY last_message_at DESC, created_at DESC
    </select>

    <insert id="save"
            parameterType="org.jejuro.miraero.domain.aicoach.domain.AiCoachConversation"
            useGeneratedKeys="true"
            keyProperty="aiCoachConversationId"
            keyColumn="ai_coach_conversation_id">
        INSERT INTO ai_coach_conversation (
            user_id,
            title
        )
        VALUES (
            #{userId},
            #{title}
        )
    </insert>

    <select id="findByIdAndUserId" resultMap="aiCoachConversationResultMap">
        SELECT
            <include refid="conversationColumns"/>
        FROM ai_coach_conversation
        WHERE user_id = #{userId}
          AND ai_coach_conversation_id = #{conversationId}
    </select>

    <delete id="deleteByIdAndUserId">
        DELETE FROM ai_coach_conversation
        WHERE user_id = #{userId}
          AND ai_coach_conversation_id = #{conversationId}
    </delete>

    <update id="updateTitle">
        UPDATE ai_coach_conversation
        SET title = #{title}
        WHERE user_id = #{userId}
          AND ai_coach_conversation_id = #{conversationId}
    </update>

    <update id="updateConversationSummary">
        UPDATE ai_coach_conversation
        SET conversation_summary = #{conversationSummary}
        WHERE user_id = #{userId}
          AND ai_coach_conversation_id = #{conversationId}
    </update>

    <update id="updateLastMessageAt">
        UPDATE ai_coach_conversation
        SET last_message_at = #{lastMessageAt}
        WHERE user_id = #{userId}
          AND ai_coach_conversation_id = #{conversationId}
    </update>

</mapper>
