<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.jejuro.miraero.domain.aicoach.mapper.AiCoachConversationMapper">

    <sql id="conversationColumns">
        ai_coach_conversation_id AS aiCoachConversationId,
        user_id AS userId,
        title,
        last_message_at AS lastMessageAt,
        created_at AS createdAt,
        updated_at AS updatedAt
    </sql>

    <select id="findLatestByUserId" resultType="map">
        SELECT
            <include refid="conversationColumns"/>
        FROM ai_coach_conversation
        WHERE user_id = #{userId}
        ORDER BY last_message_at DESC, created_at DESC
        LIMIT 1
    </select>

    <select id="findAllByUserId" resultType="map">
        SELECT
            <include refid="conversationColumns"/>
        FROM ai_coach_conversation
        WHERE user_id = #{userId}
        ORDER BY last_message_at DESC, created_at DESC
    </select>

    <insert id="save" parameterType="map" useGeneratedKeys="true" keyProperty="aiCoachConversationId">
        INSERT INTO ai_coach_conversation (
            user_id,
            title
        )
        VALUES (
            #{userId},
            #{title}
        )
    </insert>

    <select id="findByIdAndUserId" resultType="map">
        SELECT
            <include refid="conversationColumns"/>
        FROM ai_coach_conversation
        WHERE user_id = #{userId}
          AND ai_coach_conversation_id = #{conversationId}
    </select>

    <delete id="deleteByIdAndUserId">
        DELETE FROM ai_coach_conversation
        WHERE user_id = #{userId}
          AND ai_coach_conversation_id = #{conversationId}
    </delete>

    <update id="updateTitle">
        UPDATE ai_coach_conversation
        SET title = #{title}
        WHERE user_id = #{userId}
          AND ai_coach_conversation_id = #{conversationId}
    </update>

    <update id="updateLastMessageAt">
        UPDATE ai_coach_conversation
        SET last_message_at = #{lastMessageAt}
        WHERE user_id = #{userId}
          AND ai_coach_conversation_id = #{conversationId}
    </update>

</mapper>
