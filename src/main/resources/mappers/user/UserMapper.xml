<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.jejuro.miraero.domain.user.mapper.UserMapper">

  <insert id="save"
    keyProperty="userId"
    parameterType="org.jejuro.miraero.domain.user.model.User"
    useGeneratedKeys="true">
    INSERT INTO miraero_user (
    name,
    birth_date,
    company_name,
    monthly_income,
    email,
    password_hash,
    kb_pay_id
    )
    VALUES (
    #{name},
    #{birthDate},
    #{companyName},
    #{monthlyIncome},
    #{email},
    #{passwordHash},
    #{kbPayId}
    )
  </insert>

  <select id="existsByEmail"
    parameterType="string"
    resultType="boolean">
    SELECT EXISTS (
    SELECT 1
    FROM miraero_user
    WHERE email = #{email}
    )
  </select>
</mapper>
