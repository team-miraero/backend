# Database
spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3307/miraero?serverTimezone=Asia/Seoul&characterEncoding=UTF-8}
spring.datasource.username=${DB_USERNAME:miraero}
spring.datasource.password=${DB_PASSWORD}

# Redis
redis.host=${REDIS_HOST:127.0.0.1}
redis.port=${REDIS_PORT:6379}
redis.password=${REDIS_PASSWORD:}

# CORS
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:5173}

# MyData Mock Server
mydata.api.base-url=${MYDATA_API_BASE_URL:http://localhost:9000}
